---
id: setup
title: Setup and Configure
sidebar_position: 2
tags:
  - setup
  - configuration
  - initialization
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Setup

Foundation Nova supports two starting paths. Use the runner for a one-off. Install into project for configs you can commit.

## Requirements

- Node.js <abbr title="Long-Term Support">LTS</abbr>
- <abbr title="Node Package Manager">npm</abbr>

:::tip Quick Check
Run this command below to check the installed versions of your Node.js, npm, operating system, installed browsers, and other dev tools directly:

```bash
npx @cbnventures/foundation-nova@latest version --all
```
:::

## Choose Your Path

Choose **Run once (npx)** to try Nova or make a one-off change, or **Install into project** to use preset configs and run commands any time.

<Tabs groupId="setup-mode">
  <TabItem value="runner" label="Run once (npx)">

    View the built-in help from the command below, or see **[CLI](/docs/cli/)**.

    ```bash
    npx @cbnventures/foundation-nova@latest
    ```

  </TabItem>
  <TabItem value="local" label="Install into project">

    First, Install Nova.

    ```bash
    npm i -D @cbnventures/foundation-nova@latest
    ```

    Second, configure **TypeScript** though the `tsconfig.json` file. See **[TSConfig best practices](/docs/configuration/tsconfig/best-practices)**.

    ```json
    {
      "extends": [
      "./src/presets/tsconfig/dx-essentials.json",
      "./src/presets/tsconfig/dx-strict.json"
      ]
    }
    ```

    Third, configure **ESLint** through the `eslint.config.mts` file. See **[ESLint best practices](/docs/configuration/eslint/best-practices)**.

    ```javascript
    import { dxCodeStyle, scopeIgnores } from '@cbnventures/foundation-nova/eslint';

    export default [
      ...scopeIgnores,
      ...dxCodeStyle,
    ];
    ```

    Optionally, you can use the shorthand command for the **[CLI](/docs/cli/)**:

    ```bash
    nova
    ```

  </TabItem>
</Tabs>

---

## Next Steps

- See **[CLI](/docs/cli/)** for generators and project scaffolding.
- See **[Configuration](/docs/configuration/)** to choose ESLint and TypeScript scenarios.
- See **[Examples](/docs/examples/)** for sample configurations and layouts.
